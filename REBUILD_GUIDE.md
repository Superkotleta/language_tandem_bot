# Руководство по пересборке микросервисов

Это руководство поможет вам быстро и эффективно пересобирать микросервисы.

## 🚀 Быстрые команды

### Исправление зависимостей

```bash
# Исправить зависимости всех сервисов
make fix-deps
```

### Пересборка всех сервисов

```bash
# Простая пересборка
make rebuild
```

### Пересборка отдельных сервисов

```bash
# Profile Service
make rebuild-profile

# Bot Service
make rebuild-bot

# API Gateway
make rebuild-gateway
```

## 📋 Детальные команды

### Сборка

```bash
# Собрать все сервисы
make build

# Пересобрать все сервисы (с очисткой кэша)
make rebuild
```

### Управление сервисами

```bash
# Запустить все сервисы
make run

# Остановить все сервисы
make stop

# Перезапустить все сервисы
make restart

# Показать статус сервисов
make status
```

### Мониторинг

```bash
# Проверить здоровье сервисов
make health

# Показать логи всех сервисов
make logs

# Показать логи конкретного сервиса
make logs-profile
make logs-bot
make logs-gateway
```

### Telegram Bot

```bash
# Запустить polling (в отдельном терминале)
make poll

# Отправить тестовое сообщение
make test-bot

# Получить информацию о боте
make bot-info
```

### Утилиты

```bash
# Очистить контейнеры и образы
make clean

# Запустить тесты
make test

# Показать справку
make help
```

## 🆘 Устранение неполадок

### Ошибки сборки

- **Проблема**: `missing go.sum entry` или другие ошибки зависимостей.
- **Решение**: Запустите `make fix-deps` для исправления зависимостей. Затем повторите пересборку.

### Сервис не запускается после пересборки

- **Проблема**: Контейнер не стартует или сразу падает.
- **Решение**: Проверьте логи сервиса: `make logs-bot` (замените `bot` на нужный сервис).

### Медленная пересборка

- **Проблема**: Docker пересобирает все слои.
- **Решение**: Используйте `make rebuild` для быстрой пересборки только измененных сервисов.

## 📊 Мониторинг

После пересборки всегда проверяйте статус сервисов:

```bash
# Проверка здоровья всех сервисов
make health

# Просмотр логов всех сервисов
make logs
```

## 🎯 Рекомендуемый workflow

1. **Разработка**:

   ```bash
   make run          # Запустить сервисы
   make poll         # Запустить polling (в отдельном терминале)
   ```

2. **После изменений в коде**:

   ```bash
   make rebuild-bot  # Пересобрать конкретный сервис
   make health       # Проверить здоровье
   ```

3. **При проблемах с зависимостями**:

   ```bash
   make fix-deps     # Исправить зависимости
   make rebuild      # Пересобрать все
   ```

4. **Остановка**:

   ```bash
   make stop         # Остановить все сервисы
   ```

## 💡 Полезные советы

- Используйте `make help` для просмотра всех доступных команд
- Используйте `make help-verbose` для подробной справки
- Всегда проверяйте `make health` после пересборки
- Для отладки используйте `make logs-<service>` для конкретного сервиса
- Telegram polling (`make poll`) должен работать в отдельном терминале
