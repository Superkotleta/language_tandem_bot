# 🤝 Contributing to Language Exchange Bot

Спасибо за интерес к проекту Language Exchange Bot! Мы приветствуем вклад от сообщества.

## 📋 Содержание

- [Code of Conduct](#code-of-conduct)
- [Как внести вклад](#contributing)
- [Процесс разработки](#development-process)
- [Стандарты кода](#code-standards)
- [Тестирование](#testing)
- [Документация](#documentation)
- [Pull Request Process](#pull-request-process)

## 📜 Code of Conduct {#code-of-conduct}

Этот проект следует [Contributor Covenant Code of Conduct](CODE_OF_CONDUCT.md). Участвуя в проекте, вы соглашаетесь соблюдать его условия.

## 🚀 Как внести вклад {#contributing}

### Типы вкладов

Мы приветствуем следующие типы вкладов:

- 🐛 **Bug fixes** - исправление ошибок
- ✨ **New features** - новые функции
- 📚 **Documentation** - улучшение документации
- 🧪 **Tests** - добавление тестов
- 🔧 **Refactoring** - улучшение кода
- 🌐 **Translations** - переводы интерфейса
- 📊 **Performance** - оптимизация производительности

### Процесс внесения изменений

1. **Fork репозитория**
2. **Создайте feature branch**
3. **Внесите изменения**
4. **Добавьте тесты**
5. **Обновите документацию**
6. **Создайте Pull Request**

## 🔄 Процесс разработки {#development-process}

### 1. Настройка окружения

```bash
# Fork и клонирование
git clone https://github.com/your-username/language-tandem-bot.git
cd language-tandem-bot

# Добавление upstream
git remote add upstream https://github.com/original-org/language-tandem-bot.git

# Установка зависимостей
go mod download
```

### 2. Создание feature branch

```bash
# Обновление main ветки
git checkout main
git pull upstream main

# Создание feature branch
git checkout -b feature/your-feature-name

# Или для bug fix
git checkout -b fix/issue-number-description
```

### 3. Внесение изменений

```bash
# Внесите изменения в код
# Добавьте тесты
# Обновите документацию

# Проверка кода
go fmt ./...
go vet ./...
golangci-lint run

# Запуск тестов
go test ./...
```

### 4. Коммиты

```bash
# Добавление изменений
git add .

# Коммит с описательным сообщением
git commit -m "feat: add new user registration endpoint"

# Push в ваш fork
git push origin feature/your-feature-name
```

## 📝 Стандарты кода {#code-standards}

### Commit Messages

Используйте [Conventional Commits](https://www.conventionalcommits.org/) формат:

```text
type(scope): description

feat(api): add new endpoint for user stats
fix(cache): resolve memory leak in Redis client
docs(readme): update installation instructions
test(cache): add unit tests for cache service
refactor(database): simplify user queries
```

#### Типы коммитов

- `feat`: новая функция
- `fix`: исправление ошибки
- `docs`: изменения в документации
- `style`: форматирование кода
- `refactor`: рефакторинг
- `test`: добавление тестов
- `chore`: обновление зависимостей

### Go Code Style

#### 1. Форматирование

```bash
# Автоматическое форматирование
go fmt ./...

# Импорты
goimports -w .
```

#### 2. Naming Conventions

```go
// Хорошо
func GetUserByTelegramID(id int64) (*User, error) {}
func (s *Service) HandleMessage(msg string) error {}

// Плохо
func getUser(id int64) (*User, error) {}
func (s *Service) handleMsg(msg string) error {}
```

#### 3. Комментарии

```go
// Package cache provides caching functionality.
package cache

// Service provides caching operations.
type Service struct {
    // cache stores the cached data
    cache map[string]interface{}
}

// Get retrieves a value from cache.
// Returns the value and a boolean indicating if the key was found.
func (s *Service) Get(key string) (interface{}, bool) {
    // Implementation
}
```

#### 4. Error Handling

```go
// Хорошо
func ProcessUser(user *User) error {
    if err := validateUser(user); err != nil {
        return fmt.Errorf("failed to validate user: %w", err)
    }
    
    if err := saveUser(user); err != nil {
        return fmt.Errorf("failed to save user: %w", err)
    }
    
    return nil
}

// Плохо
func ProcessUser(user *User) error {
    validateUser(user) // Игнорирование ошибки
    saveUser(user)     // Игнорирование ошибки
    return nil
}
```

## 🧪 Тестирование {#testing}

### Требования к тестам

- **Покрытие**: Минимум 70% для новых компонентов
- **Unit тесты**: Для всех публичных функций
- **Integration тесты**: Для критических путей
- **E2E тесты**: Для пользовательских сценариев

### Запуск тестов

```bash
# Все тесты
go test ./...

# С покрытием
go test ./... -coverprofile=coverage.out
go tool cover -html=coverage.out

# Конкретный пакет
go test ./internal/cache/... -v

# С race detector
go test ./... -race
```

### Написание тестов

```go
func TestService_Method(t *testing.T) {
    // Arrange
    service := NewService()
    input := "test input"
    expected := "expected output"
    
    // Act
    result, err := service.Method(input)
    
    // Assert
    assert.NoError(t, err)
    assert.Equal(t, expected, result)
}
```

## 📚 Документация {#documentation}

### Обновление документации

При внесении изменений обновляйте соответствующую документацию:

- **README.md** - для изменений в setup/installation
- **API docs** - для изменений в API
- **Developer Guide** - для изменений в development process
- **Code comments** - для изменений в коде

### Типы документации

1. **Inline комментарии** - для сложной логики
2. **Package документация** - для каждого пакета
3. **API документация** - для HTTP endpoints
4. **User документация** - для пользователей
5. **Developer документация** - для разработчиков

## 🔄 Pull Request Process {#pull-request-process}

### 1. Создание PR

```bash
# Убедитесь, что все тесты проходят
go test ./...

# Проверьте линтер
golangci-lint run

# Создайте PR в GitHub
```

### 2. Описание PR

Используйте шаблон:

```markdown
## Описание
Краткое описание изменений

## Тип изменений
- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation update

## Тестирование
- [ ] Unit тесты добавлены/обновлены
- [ ] Integration тесты проходят
- [ ] E2E тесты проходят
- [ ] Manual тестирование выполнено

## Checklist
- [ ] Код соответствует стандартам проекта
- [ ] Self-review выполнен
- [ ] Документация обновлена
- [ ] Коммиты следуют conventional commits
```

### 3. Review Process

1. **Автоматические проверки** должны пройти
2. **Code review** от минимум одного maintainer
3. **Тесты** должны проходить
4. **Документация** должна быть обновлена

### 4. После merge

- Feature branch будет удален
- Изменения попадут в main ветку
- Автоматически запустится CI/CD pipeline

## 🐛 Сообщение об ошибках

### Создание Bug Report

Используйте шаблон:

```markdown
## Описание ошибки
Четкое и краткое описание ошибки

## Шаги для воспроизведения
1. Перейти к '...'
2. Нажать на '...'
3. Прокрутить до '...'
4. Увидеть ошибку

## Ожидаемое поведение
Четкое описание того, что должно было произойти

## Скриншоты
Если применимо, добавьте скриншоты

## Окружение
- OS: [e.g. Ubuntu 20.04]
- Go version: [e.g. 1.25.0]
- Docker version: [e.g. 20.10.0]

## Дополнительная информация
Любая другая информация об ошибке
```

## ✨ Предложения по улучшению

### Создание Feature Request

```markdown
## Описание функции
Четкое и краткое описание желаемой функции

## Проблема, которую это решает
Какая проблема решается этой функцией?

## Предлагаемое решение
Четкое описание того, что вы хотите, чтобы произошло

## Альтернативы
Опишите любые альтернативные решения

## Дополнительная информация
Любая другая информация о feature request
```

## 🏷️ Labels

Мы используем следующие labels:

- `bug` - ошибка
- `enhancement` - улучшение
- `documentation` - документация
- `good first issue` - подходит для новичков
- `help wanted` - нужна помощь
- `priority: high` - высокий приоритет
- `priority: low` - низкий приоритет

## 📞 Получение помощи

### Где получить помощь

1. **GitHub Issues** - для багов и feature requests
2. **GitHub Discussions** - для вопросов и обсуждений
3. **Code Review** - для вопросов по коду
4. **Email** - для приватных вопросов

### Как задать хороший вопрос

1. **Поищите** в существующих issues
2. **Опишите проблему** четко и подробно
3. **Приложите** минимальный воспроизводимый пример
4. **Укажите** версию и окружение

## 🎉 Признание вкладов

Мы ценим все вклады в проект:

- **Contributors** будут добавлены в README
- **Significant contributions** будут отмечены особо
- **Code reviews** также считаются вкладом

## 📄 Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш вклад будет лицензирован под той же лицензией, что и проект.

---

## 🎉 Спасибо за ваш вклад

Если у вас есть вопросы, не стесняйтесь создать issue или связаться с командой разработки.
