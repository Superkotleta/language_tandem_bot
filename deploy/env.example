# PostgreSQL Configuration
POSTGRES_DB=language_exchange
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here

# pgAdmin Configuration
PGADMIN_EMAIL=admin@localhost.local
PGADMIN_PASSWORD=admin

# Telegram Bot Configuration
TELEGRAM_TOKEN=your_telegram_bot_token_here

# Note: DATABASE_URL is constructed automatically in docker-compose.yml
# If you need to override it, uncomment and modify:
# DATABASE_URL=postgresql://postgres:your_secure_password_here@db:5432/language_exchange?sslmode=disable

