# Быстрый старт

Этот файл содержит все необходимые команды для работы с Language Exchange Bot.

## 🚀 Запуск системы

### 1. Запуск всех сервисов

```bash
make run
```

### 2. Запуск Telegram polling (в отдельном терминале)

```bash
make poll
```

## 🔧 Управление сервисами

### Пересборка сервисов

```bash
# Все сервисы
make rebuild

# Конкретный сервис
make rebuild-bot
make rebuild-profile
make rebuild-gateway
```

### Исправление зависимостей

```bash
# Все сервисы
make fix-deps
```

## 📊 Мониторинг

### Проверка здоровья

```bash
make health
```

### Просмотр логов

```bash
# Все сервисы
make logs

# Конкретный сервис
make logs-bot
make logs-profile
make logs-gateway
```

## 🤖 Работа с Telegram ботом

### Запуск polling (обязательно для работы бота)

```bash
make poll
```

### Тестирование бота

```bash
# Отправить тестовое сообщение
make test-bot

# Получить информацию о боте
make bot-info
```

Или просто напишите боту в Telegram команду `/start` - он должен ответить приветственным сообщением.

## 🛠️ Полезные команды

### Остановка сервисов

```bash
make stop
```

### Очистка

```bash
make clean
```

### Тестирование

```bash
make test
```

### Справка

```bash
# Основная справка
make help

# Подробная справка
make help-verbose
```

## 📁 Структура проекта

```shell
language_exchange_bot/
├── services/
│   ├── profile/          # Profile Service
│   ├── bot/              # Bot Service
│   └── api-gateway/      # API Gateway
├── docs/                 # Документация
├── docker-compose.mvp.yml
├── Makefile
└── README.MVP.md
```

## 🆘 Устранение неполадок

### Бот не отвечает

1. Проверьте, запущен ли polling: `make poll`
2. Проверьте логи: `make logs-bot`
3. Проверьте токен в .env файле

### Ошибки сборки

1. Исправьте зависимости: `make fix-deps`
2. Пересоберите сервисы: `make rebuild`

### Проблемы с портами

1. Остановите сервисы: `make stop`
2. Проверьте занятые порты: `netstat -tulpn | grep :808`
3. Запустите заново: `make run`
